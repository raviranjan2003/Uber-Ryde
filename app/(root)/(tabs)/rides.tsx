import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
    return (
        <SafeAreaView>
            <View>
                <Text>This is my Rides!</Text>
            </View>
        </SafeAreaView>
    )
}

export default Rides;